// Generated by CoffeeScript 1.4.0
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Application.Models.Dataset = (function(_super) {

    __extends(Dataset, _super);

    function Dataset() {
      return Dataset.__super__.constructor.apply(this, arguments);
    }

    Dataset.prototype.defaults = function() {
      var hash,
        _this = this;
      hash = {};
      Application.Schema.basic.forEach(function(field) {
        return hash[field.get('json')] = null;
      });
      hash.title = 'Dataset';
      return hash;
    };

    return Dataset;

  })(Backbone.Model);

  Application.Collections.Datasets = (function(_super) {

    __extends(Datasets, _super);

    function Datasets() {
      return Datasets.__super__.constructor.apply(this, arguments);
    }

    Datasets.prototype.model = Application.Models.Dataset;

    Datasets.prototype.getJSON = function() {
      return JSON.stringify(this.toJSON());
    };

    Datasets.prototype.getHTML = function() {
      return this.getTemplated('html');
    };

    Datasets.prototype.getXML = function() {
      return "<datasets>\n" + this.getTemplated('xml') + "</datasets>\n";
    };

    Datasets.prototype.getTemplated = function(type) {
      var compiled, output;
      output = '';
      compiled = _.template($('#' + type + '_template').html());
      this.each(function(dataset) {
        return output += compiled({
          schema: Application.Schema.basic.toJSON(),
          dataset: dataset.toJSON()
        });
      });
      return output;
    };

    return Datasets;

  })(Backbone.Collection);

}).call(this);
